diff --git a/configure b/configure
index 2acc4d14..47e744c2 100755
--- a/configure
+++ b/configure
@@ -449,6 +449,7 @@ rbd=""
 smartcard=""
 libusb=""
 usb_redir=""
+stubdom="no"
 opengl=""
 opengl_dmabuf="no"
 cpuid_h="no"
@@ -1398,6 +1399,8 @@ for opt do
   ;;
   --enable-usb-redir) usb_redir="yes"
   ;;
+  --enable-stubdom) stubdom="yes"
+  ;;
   --disable-zlib-test) zlib="no"
   ;;
   --disable-lzo) lzo="no"
@@ -1904,6 +1907,7 @@ disabled with --disable-FEATURE, default is enabled if available:
   usb-redir       usb network redirection support
   lzo             support of lzo compression library
   snappy          support of snappy compression library
+  enable-stubdom  support for Xen stubdom
   bzip2           support of bzip2 compression library
                   (for reading bzip2-compressed dmg images)
   lzfse           support of lzfse compression library
@@ -7638,6 +7642,11 @@ if test "$rbd" = "yes" ; then
   echo "RBD_LIBS=$rbd_libs" >> $config_host_mak
 fi
 
+# stubdom support
+if test "$stubdom" = "yes"; then
+  echo "CONFIG_STUBDOM=y" >> $config_host_mak
+fi
+
 echo "CONFIG_COROUTINE_BACKEND=$coroutine" >> $config_host_mak
 if test "$coroutine_pool" = "yes" ; then
   echo "CONFIG_COROUTINE_POOL=1" >> $config_host_mak
